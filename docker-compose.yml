services:
  vending-machine:
    build: ./
    ports:
      - "8080:8080"
    volumes:
      - .:/app
      - vendor:/app/vendor
  redis:
    image: redis:7
    ports:
      - "6379:6379"

volumes:
  vendor:
